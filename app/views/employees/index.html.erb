<p style="color: green"><%= notice %></p>

<h1><%= I18n.t(:employees) %></h1>

<div id="employees">
  <% @employees.each do |employee| %>
    <% if employee.photo.attached? %>
      <%= image_tag(employee.photo, alt: employee.full_name, class: "employee-photo") %>
    <% end %>
    <%= link_to employee.full_name, employee_path(employee) %>
    <%= employee.department %>
  <% end %>
</div>

<%= link_to I18n.t(:add_employee), new_employee_path %>
<hr>

<%= link_to "English", url_for(locale: "en") %>
<%= link_to "Português", url_for(locale: "pt-BR") %>
