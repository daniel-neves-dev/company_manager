<p style="color: green"><%= notice %></p>

<%= render @employee %>

<div>
  <%= link_to "Edit", edit_employee_path(@employee) %> |
  <%= link_to "Back", employees_path %>

  <%= button_to "Delete", @employee, method: :delete,data: { turbo_confirm: "Are you sure?" } %>
</div>
