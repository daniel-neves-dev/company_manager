<nav class="border-gray-200 bg-gray-800">
  <div class="max-w-screen-xl mx-auto p-4 flex flex-wrap justify-between items-center">

    <div class="w-full md:w-auto md:order-1" id="navbar-search">
      <ul class="flex flex-col p-4 md:p-0 mt-4 md:space-x-8 md:flex-row md:mt-0 md:border-0 font-medium bg-gray-800">
        <li>
          <%= link_to "Employees", employees_path, class: "block py-2 px-3 text-white bg-blue-700 rounded md:bg-transparent md:text-blue-700 md:p-0 md:dark:text-blue-500", aria: { current: "page" } %>
        </li>
        <li>
          <%= link_to "English",url_for(locale: "en") , class: "block py-2 px-3 text-white bg-blue-700 rounded md:bg-transparent md:text-blue-700 md:p-0 md:dark:text-blue-500", aria: { current: "page" } %>
        </li>
        <li>
          <%= link_to "Português", url_for(locale: "pt-BR") , class: "block py-2 px-3 text-white bg-blue-700 rounded md:bg-transparent md:text-blue-700 md:p-0 md:dark:text-blue-500", aria: { current: "page" } %>
        </li>
        <li>
          <%= link_to I18n.t(:add_employee), new_employee_path , class: "block py-2 px-3 text-white bg-blue-700 rounded md:bg-transparent md:text-blue-700 md:p-0 md:dark:text-blue-500", aria: { current: "page" } %>
        </li>
      </ul>
    </div>

    <div class="flex flex-col w-full lg:w-1/2 md:order-2">
      <%= search_form_for @ransack_employees, url: employees_path(locale: I18n.locale), html: { method: :get, class: "w-full" } do |f| %>
        <div class="relative w-full">
          <div class="absolute inset-y-0 left-0 flex items-center pl-3 pointer-events-none">
            <svg class="w-5 h-5 text-gray-400" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20">
              <!-- SVG path for search icon -->
            </svg>
          </div>
          <%= f.search_field :first_name_or_last_name_cont, id: "q_first_name_or_last_name_cont", class: "block w-full p-2 pl-10 pr-16 text-sm text-gray-900 bg-gray-700 border border-gray-600 rounded-md focus:ring-blue-500 focus:border-blue-500 dark:placeholder-gray-400 dark:text-white", autocomplete: "off", placeholder: "Search Employees..." %>
          <button type="submit" class="absolute right-0 top-0 bottom-0 px-4 py-2 text-sm text-white bg-blue-700 rounded-r-md hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300">Search</button>
        </div>
        <div class="mt-2 grid grid-cols-1 gap-2 md:grid-cols-2">
          <div>
            <%= f.select :department_eq,
                         Employee.departments.keys.map { |key| [I18n.t("activerecord.attributes.employee.departments.#{key}"), Employee.departments[key]] },
                         { include_blank: 'Any Department' },
                         { class: "form-select bg-gray-700 border border-gray-600 text-white rounded-md text-sm w-full" } %>

          </div>
          <div>
            <%= f.select :role_eq,
                         Employee.roles.keys.map { |key| [I18n.t("activerecord.attributes.employee.roles.#{key}"), Employee.roles[key]] },
                         { include_blank: 'Any Role' },
                         { class: "form-select bg-gray-700 border border-gray-600 text-white rounded-md text-sm w-full" } %>
          </div>
        </div>
      <% end %>
    </div>

  </div>
</nav>
